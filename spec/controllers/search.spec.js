describe('search controller', function() {
    it('should load the search controller when the application is accessed');
    describe('search', function() {
        it('should have a method to allow for searching the web');
        it('should return an array of results');
        it('results should have a title, a description and a url to link to the site');
    });
});